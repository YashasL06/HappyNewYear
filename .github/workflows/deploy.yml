jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:  # REQUIRED for OIDC token
      id-token: write
      contents: read
    steps:
    - uses: actions/checkout@v4
    - name: Configure AWS Credentials  # Updated above
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-region: us-east-1
        role-to-assume: arn:aws:iam::515421558980:role/GitHubActionsRole
        audience: sts.amazonaws.com
        role-session-name: GitHubActionsSession
    # Test: aws sts get-caller-identity
    - name: Deploy via SSM
      run: |
        aws ssm start-session --target i-YOUR-ID --document-name AWS-StartSSHSession \
<<<<<<< HEAD
          --parameters '{"portNumber":["22"]}'
=======
          --parameters '{"portNumber":["22"]}'
>>>>>>> 0034495ca7f69185e2c8a51d0f297beef9352f96
